1. Part catalog — Where should part search/picker data come from? (Bricklink, Rebrickable, a CSV you maintain, or something else?)

I have a color picker input control that should be reused.  It is currently implemented in my chrome extension code.

There should be a kind of api call that can be made to search for parts.  I will need to provide more information to you for that.  I can point you to the chrome extension code that includes a similar feature.

2. Part-out list — How do you get the official list for a set today? (Bricklink part-out export, spreadsheet, other?) What format should MVP support?

Bricklink has a part-out feature where you provide the set number and some optional configuration (what prices to use, if the parts are new or used, and whether to overwrite the current value in the store inventory or to consolidate)

I already have a Chrome extension that could be updated to scrape the part-out list entries and export as a JSON. Or I could create a service call that takes a cookie session that can be used by this new application to fetch the part-out list more directly.  I can point you to the chrome extension code that includes a similar feature.

3. Bricklink at the end — For MVP, is export of the reconciled list enough, or do you need direct submit to Bricklink in this Feature?

I would like to export to XML and use the Bricklink bulk update tool.  I can point you to the chrome extension code that includes a similar feature.

4. Workers — How should people identify themselves? (Pick name at join, simple PIN, no auth on local network, etc.)

Pick name at join

5. Multi-color cups — When one physical cup has multiple colors of the same part, should workers enter separate lot lines per color, or is there another rule you use today?

There should be two buttons on the form
- Save
- Save and Add Another

When "Save and Add Another" is pressed, the current lot is saved and a new form is opened, pre-populating the part id with the value of the previous form

Keep the form/view simple by not supporting multiple lots per form

Another view should exist to represent a list of all the cups
- clicking on a cup
  - where there is only one color for 1 or more parts should open the lot in the form
  - where there is more than one color for 2 or more parts should prompt the worker for which lot should be loaded in the form; then open the lot in the form

6. Storage locations — Where do drawer/bin location values come from? (Fixed list you configure, labels scanned/typed per session, imported from elsewhere?)

The storage location information should be included in the part-out list from Bricklink.  It comes from the "Remarks" (aka "My Remarks", location) metadata.
